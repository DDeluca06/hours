{
  "name": "@hours/connector-google-sheets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "probe": "tsx --env-file-if-exists=../../.env src/probe.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@hours/config": "workspace:*",
    "@hours/core": "workspace:*",
    "googleapis": "^144.0.0"
  }
}
